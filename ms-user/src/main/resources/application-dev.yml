spring:
  datasource:
    url: jdbc:postgresql://localhost:5440/ms-user
    username: ms-user
    password: ms-user
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
